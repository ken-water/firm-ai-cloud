# v0.1.24 Release Gate Checklist

Date: 2026-03-19
Version: v0.1.24

## 1. Preconditions

1. API and web-console run against migrated database.
2. `v0.1.23` release is complete and synchronized.
3. Demo account set (admin/operator/read-only) is available.
4. `v0.1.24` scope is mapped to strategic baseline document:
   - `docs/62-smb-intelligent-cloud-product-strategy-baseline.md`

## 2. Required Planning Evidence

1. Release planning issue references IA + visual + topology + demo scenario outcomes.
2. Scope includes explicit first-glance usability targets.
3. Scope includes deterministic demo walkthrough and evidence points.

## 3. Required Validation Coverage

1. IA entry flow is validated for module discoverability.
2. Visual cockpit hierarchy is validated for KPI-first readability.
3. Topology readability improvements are validated with deterministic ordering/grouping checks.
4. Demo scenario pack is validated for repeatable walkthrough completion.

## 4. Required Artifacts

- `summary.json`
- `summary.md`
- `artifact-index.json`
- stage logs (`*.log`)

## 5. Release Gates

Before publish:

```bash
cargo check --workspace
cargo test -p api
cd apps/web-console && npm run check:ui
```

Publish sync:

```bash
bash scripts/release-github-issues-check.sh --version 0.1.24 --notes-file release-notes/v0.1.24.md
make release-publish-dry VERSION=0.1.24
make release-publish VERSION=0.1.24
make release-check VERSION=0.1.24
```
