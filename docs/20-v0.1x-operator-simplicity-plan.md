# CloudOps One v0.1.x Planning (Operator Simplicity and Efficiency)

Date: 2026-03-06
Version: v0.3
Target track: `v0.1.0` -> `v0.1.1` -> `v0.1.2` -> `v0.1.3` -> `v0.1.4`

## 1. Planning Goal

This plan drives CloudOps One toward a practical outcome:

- Small-to-mid enterprises (dozens to thousands of assets)
- Operators can complete daily operations safely with minimal technical background
- Core incident loop is simple, guided, and auditable

## 2. Strategy

Use incremental milestones instead of one big release:

1. `v0.1.0`: build onboarding and alert-loop foundation.
2. `v0.1.1`: add one-click operations and daily guided cockpit.
3. `v0.1.2`: harden enterprise security and scale readiness.
4. `v0.1.3`: operator no-code execution loop + 10k-scale evidence closure.
5. `v0.1.4`: safe-by-default operations governance + long-soak and multi-region reliability evidence.

## 3. GitHub Issue Map

### 3.1 v0.1.0 (Foundation: onboarding + alert loop)

| Track | Issue | Deliverable |
| --- | --- | --- |
| Planning | #72 | Issue-first roadmap and dependency order |
| Setup API | #73 | Setup preflight and integration checklist endpoints |
| Setup UI | #74 | First-run onboarding wizard for non-technical users |
| Alert API | #75 | Unified alert center APIs (`list/ack/close`) |
| Alert UI | #76 | Alert center page with bulk actions and drill panel |
| Automation | #77 | Alert-to-ticket rule engine with default templates |
| Release | #78 | v0.1.0 release artifacts and validation closure |

### 3.2 v0.1.1 (Operations UX: guided actions)

| Track | Issue | Deliverable |
| --- | --- | --- |
| Playbooks | #79 | One-click safe operation playbook library |
| Cockpit | #80 | Daily prioritized operations cockpit |
| Topology Diagnostics | #81 | Guided diagnostics panel from topology links |
| Release | #82 | v0.1.1 release artifacts and validation closure |

### 3.3 v0.1.2 (Enterprise hardening + scale)

| Track | Issue | Deliverable |
| --- | --- | --- |
| Enterprise Identity | #83 | LDAP/AD integration and role mapping |
| Local Auth Hardening | #84 | MFA/TOTP + session/lockout policy controls |
| Scale Reliability | #85 | 1k-5k asset benchmark profile and SLO gate |
| Release | #86 | v0.1.2 release artifacts and readiness closure |

### 3.4 v0.1.3 (Operator simplicity + production hardening evidence)

| Track | Issue | Deliverable |
| --- | --- | --- |
| Identity Hardening | #105 | LDAP live + Argon2id + MFA recovery baseline (parent) |
| Ops Simplicity Parent | #106 | No-code onboarding + guided remediation workflows |
| Scale Evidence Parent | #107 | 10k profile validation and capacity guidance |
| Release Parent | #108 | v0.1.3 closure and risk handoff |
| Setup Templates | #112 | No-code bootstrap templates for operators |
| Remediation Flow | #113 | Alert-to-playbook one-click remediation |
| Guided Checklist | #114 | Daily checklist tracking and exception audit |
| 10k Profile | #115 | `scale-10k` benchmark profile + fixtures |
| CI/Regression | #116 | Benchmark artifact publishing and regression guard |
| Release Closure | #117 | v0.1.3 closure evidence and deferred-risk handoff |

### 3.5 v0.1.4 (Safe-by-default operations + stability evidence)

| Track | Issue | Deliverable |
| --- | --- | --- |
| Planning | #118 | v0.1.4 roadmap, dependency order, and release gate |
| Ops Policy | #119 | Maintenance windows and change-freeze guardrails |
| Approval Control | #120 | Two-person approval for high-risk playbook execution |
| Alert Governance | #121 | Alert dedup/suppression policy baseline with safe defaults |
| Backup/DR Automation | #122 | No-code backup policy wizard and scheduled drill orchestration |
| Weekly Digest | #123 | One-click weekly operations health digest and export |
| Long-Soak Evidence | #124 | 24h benchmark profile and drift report artifacts |
| Multi-Region Evidence | #125 | Multi-region latency/failover simulation baseline |
| Release | #126 | v0.1.4 release closure and deferred-risk handoff |

## 4. Execution Order

Recommended order:

1. Finish `#72` and lock v0.1.0 acceptance criteria.
2. Deliver setup foundations: `#73` then `#74`.
3. Deliver incident loop foundations: `#75` -> `#76` -> `#77`.
4. Close v0.1.0 with `#78`.
5. Deliver operations simplification: `#79` -> `#80` -> `#81`, then `#82`.
6. Deliver enterprise hardening and scale proof: `#83` -> `#84` -> `#85`, then `#86`.
7. Deliver v0.1.3 identity hardening parent: `#105` (`#109` -> `#110` -> `#111`).
8. Deliver operator no-code loop: `#112` -> `#113` -> `#114`, then close `#106`.
9. Deliver 10k scale evidence: `#115` -> `#116`, then close `#107`.
10. Close release evidence and risk handoff: `#117`, then close `#108`.
11. Lock v0.1.4 acceptance and dependency order with `#118`.
12. Deliver safe-by-default controls: `#119` -> `#120` -> `#121`.
13. Deliver non-technical continuity and reporting: `#122` -> `#123`.
14. Deliver release-grade stability evidence: `#124` -> `#125`, then close `#126`.

## 5. Definition of Done by Track

Each milestone is done only when:

- All milestone issues are closed.
- `cargo check --workspace`, `cargo test -p api`, and `npm run check:ui` pass.
- Release note/changelog are updated and validated.
- CI run URL(s) and benchmark evidence are linked in release artifacts.

## 6. Risks and Mitigation

| Risk | Impact | Mitigation |
| --- | --- | --- |
| Onboarding still too technical | Low adoption by non-expert users | Force wizard-based golden path and explicit remediation hints |
| Alert noise overload | Operators ignore alerts | Add dedup grouping and policy templates before cockpit rollout |
| Automation misuse | Unsafe operation changes | Use dry-run, confirmation, RBAC, and audit logging |
| Scale regressions at 1k+ assets | Unstable production behavior | Add benchmark profile + SLO gate before v0.1.2 release |
| Identity misconfiguration | Access lockout or privilege drift | Keep break-glass controls and add mapping validation checks |
| Uncontrolled high-risk change windows | Increased outage risk from mistimed actions | Enforce maintenance-window and approval policies before execute/replay |
| Reliability blind spots under long-running or cross-region load | Late regression detection near release | Add long-soak + multi-region benchmark evidence before v0.1.4 closure |

## 7. Status Snapshot (2026-03-06)

- v0.1.3 closure is complete (`#105` to `#117` closed).
- v0.1.4 planning baseline is created (`#118` to `#126`).
- Current active queue recommendation:
  1. `#119`
  2. `#120`
  3. `#121`
  4. `#122`
  5. `#123`
  6. `#124`
  7. `#125`
  8. `#126`
