# CloudOps One v0.0.5 Monitoring Bootstrap Plan

Date: 2026-03-03

This document tracks the issue-first implementation plan for v0.0.5.

## 1. Problem Inventory (Current State)

1. There is no dedicated monitoring source registry for Zabbix server/proxy onboarding.
2. Monitoring API contracts are not ready for frontend integration.
3. Web-console has no monitoring source operation surface yet.
4. Real-time alert transport baseline is still missing.

## 2. Iteration Backlog

| Iteration | GitHub Issue | Focus |
| --- | --- | --- |
| I1 | `#28` | Monitoring source registry + probe APIs |
| I2 | `#29` | Monitoring overview/layer API contracts |
| I3 | `#30` | Web-console monitoring source UX |
| I4 | `#31` | SSE stream baseline for real-time alerts |
| I5 | `#32` | Bundled Zabbix server/proxy/local-agent one-click deployment |

## 3. Iteration Rules

1. Keep each iteration independently releasable.
2. Preserve manual-trigger CI policy and local validation-first workflow.
3. Keep docs and release-facing text in English.
4. Ensure RBAC mapping and audit traceability for new protected routes.

## 4. I1 Definition of Done (`#28`)

- Monitoring source schema is available via migration.
- API endpoints exist for source list/create/probe.
- RBAC permissions and mapping are in place (`monitoring.sources.read/write`).
- Operator/viewer role behavior is covered by scripts/tests.
- Workspace checks pass.

## 5. I1 Completion Notes (`#28`)

Status: Completed on 2026-03-03.

Delivered updates:

1. Added `monitoring_sources` schema and indexes in migration `202603030001_create_monitoring_sources.sql`.
2. Added monitoring source APIs:
   - `GET /api/v1/monitoring/sources`
   - `POST /api/v1/monitoring/sources`
   - `POST /api/v1/monitoring/sources/{id}/probe`
3. Added RBAC permissions and route mapping:
   - `monitoring.sources.read`
   - `monitoring.sources.write`
4. Added monitoring permission coverage to:
   - `services/api/src/auth.rs` tests
   - `scripts/test-rbac-policy.sh`
   - `docs/10-rbac-permission-coverage.md`

Validation:

- `cargo check --workspace`
- `cargo test -p api`
- `npm run check:ui` (`apps/web-console`)

## 6. I5 Completion Notes (`#32`)

Status: Completed on 2026-03-03.

Delivered updates:

1. Extended `deploy/docker-compose.yml` with bundled Zabbix runtime:
   - `zabbix-db`
   - `zabbix-server`
   - `zabbix-web`
   - `zabbix-proxy`
   - `zabbix-agent-local`
2. Added Zabbix environment defaults in:
   - `deploy/.env.example`
   - `deploy/.env.cn.example`
3. Updated install/upgrade scripts to wait for Zabbix services and print Zabbix access endpoints.
4. Updated offline bundle builder to include all Zabbix images.
5. Updated installation/quickstart docs with:
   - Zabbix default access
   - remote agent onboarding (`Server`/`ServerActive` -> proxy endpoint)

Validation:

- `bash -n scripts/install.sh scripts/upgrade.sh scripts/build-offline-bundle.sh scripts/install-offline.sh scripts/test-rbac-policy.sh`
- `docker-compose --env-file deploy/.env.example -f deploy/docker-compose.yml config`
- `docker-compose --env-file deploy/.env.cn.example -f deploy/docker-compose.yml config`
- `cargo check --workspace`
- `cargo test -p api`
- `npm run check:ui` (`apps/web-console`)
