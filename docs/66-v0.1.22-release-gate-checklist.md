# v0.1.22 Release Gate Checklist

Date: 2026-03-18
Version: v0.1.22

## 1. Preconditions

1. API and web-console run against migrated database.
2. `v0.1.21` release is complete and synchronized.
3. One write-capable and one read-only account are available.
4. `v0.1.22` scope is mapped to strategic baseline document:
   - `docs/62-smb-intelligent-cloud-product-strategy-baseline.md`

## 2. Required Planning Evidence

1. Release planning issue references strategic pillars and SMB first-value outcomes.
2. Scope includes explicit dashboard role-template simplification.
3. Scope includes measurable cockpit readability and evidence-trust goals.

## 3. Required Validation Coverage

1. Dashboard studio baseline is validated for deterministic widget/template rendering.
2. Business topology view baseline is validated for service risk readability.
3. AI copilot intent baseline is validated for evidence completeness continuity.
4. One-screen handoff summary is validated for action-priority clarity.
5. Read-only guard paths remain enforced.

## 4. Required Artifacts

- `summary.json`
- `summary.md`
- `artifact-index.json`
- stage logs (`*.log`)

## 5. Release Gates

Before publish:

```bash
cargo check --workspace
cargo test -p api
cd apps/web-console && npm run check:ui
```

Publish sync:

```bash
bash scripts/release-github-issues-check.sh --version 0.1.22 --notes-file release-notes/v0.1.22.md
make release-publish-dry VERSION=0.1.22
make release-publish VERSION=0.1.22
make release-check VERSION=0.1.22
```
