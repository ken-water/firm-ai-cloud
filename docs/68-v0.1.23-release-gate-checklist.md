# v0.1.23 Release Gate Checklist

Date: 2026-03-18
Version: v0.1.23

## 1. Preconditions

1. API and web-console run against migrated database.
2. `v0.1.22` release is complete and synchronized.
3. One write-capable and one read-only account are available.
4. `v0.1.23` scope is mapped to strategic baseline document:
   - `docs/62-smb-intelligent-cloud-product-strategy-baseline.md`

## 2. Required Planning Evidence

1. Release planning issue references strategic pillars and SMB operation outcomes.
2. Scope includes explicit safe-action boundary requirements.
3. Scope includes target readability/trust improvements for shift handoff.

## 3. Required Validation Coverage

1. Copilot guided-action contract is validated for safety and evidence completeness.
2. Topology board baseline is validated for deterministic ordering and risk readability.
3. Handoff readiness model is validated for deterministic state derivation.
4. Cockpit integration keeps read-only/write-guard behavior intact.

## 4. Required Artifacts

- `summary.json`
- `summary.md`
- `artifact-index.json`
- stage logs (`*.log`)

## 5. Release Gates

Before publish:

```bash
cargo check --workspace
cargo test -p api
cd apps/web-console && npm run check:ui
```

Publish sync:

```bash
bash scripts/release-github-issues-check.sh --version 0.1.23 --notes-file release-notes/v0.1.23.md
make release-publish-dry VERSION=0.1.23
make release-publish VERSION=0.1.23
make release-check VERSION=0.1.23
```
