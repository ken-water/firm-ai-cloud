# v0.1.21 Release Gate Checklist

Date: 2026-03-18
Version: v0.1.21

## 1. Preconditions

1. API and web-console run against migrated database.
2. `v0.1.20` owner assignment/escalation loop release is complete and synced.
3. One write-capable and one read-only account are available.
4. `v0.1.21` scope is mapped to strategic baseline document:
   - `docs/62-smb-intelligent-cloud-product-strategy-baseline.md`

## 2. Required Planning Evidence

1. Release planning issue references strategic pillars.
2. Scope includes explicit user-visible workflow simplification.
3. Scope includes target KPI movement for adoption/response/resolution/trust.

## 3. Required Validation Coverage

1. Module-first dashboard entry readability is validated.
2. Business-centric monitoring/CMDB baseline contracts are validated.
3. Organization workflow/approval baseline guardrails are validated.
4. AI answer baseline includes evidence/source/time-range fields.
5. Read-only guard paths remain enforced.

## 4. Required Artifacts

- `summary.json`
- `summary.md`
- `artifact-index.json`
- stage logs (`*.log`)

## 5. Release Gates

Before publish:

```bash
cargo check --workspace
cargo test -p api
cd apps/web-console && npm run check:ui
```

Publish sync:

```bash
bash scripts/release-github-issues-check.sh --version 0.1.21 --notes-file release-notes/v0.1.21.md
make release-publish-dry VERSION=0.1.21
make release-publish VERSION=0.1.21
make release-check VERSION=0.1.21
```
