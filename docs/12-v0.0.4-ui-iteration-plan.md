# CloudOps One v0.0.4 UI Iteration Plan

Date: 2026-03-03

This document tracks the issue-first UI improvement process for v0.0.4.

## 1. Problem Inventory (Current State)

1. Information architecture is crowded (single-page stacked operations).
2. Visual style is inconsistent across controls and content blocks.
3. Global UX states (`loading/error/forbidden/empty`) are not consistently presented.
4. Role-based constraints exist but are not always clear in context.
5. Frontend structure is hard to evolve quickly without regressions.

## 2. Iteration Backlog

| Iteration | GitHub Issue | Focus |
| --- | --- | --- |
| I1 | `#24` | UI foundation and layout architecture |
| I2 | `#25` | CMDB assets and relation workflow UX |
| I3 | `#26` | Discovery and notification operation UX |
| I4 | `#27` | Frontend regression/quality guardrails |

## 3. Iteration Rules

1. One focused scope per iteration.
2. Keep each change set independently reversible.
3. Preserve existing backend API behavior while improving UI.
4. Keep release-facing text in English.
5. Maintain compatibility with manual-trigger CI policy.

## 4. I1 Definition of Done

- Shared design tokens are in place.
- Web-console has a stable shell layout (sidebar + main content).
- Section-level layout is visually consistent.
- Authentication/session status is visible in a dedicated topbar.
- Baseline changes compile and pass frontend build checks.

## 5. I1 Completion Notes (`#24`)

Status: Completed on 2026-03-03.

Delivered updates:

1. Extracted reusable layout primitives in `web-console`:
   - `AuthGate`
   - `AppShell`
   - `SectionCard`
2. Replaced repeated page scaffolding in `App.tsx` with the shared primitives.
3. Added baseline section header styles (`section-head`, `section-title`, `section-actions`) to keep card blocks consistent.
4. Preserved global UX status visibility through unified banner rendering in shared shell components.
5. Verified build health:
   - `npm run build` (`apps/web-console`)
   - `cargo check --workspace`

Follow-up iterations:

- `#25`: CMDB assets and relation workflow redesign.
- `#26`: Discovery and notification operation redesign.
- `#27`: Frontend regression and quality guardrails.

## 6. I2 Completion Notes (`#25`)

Status: Completed on 2026-03-03.

Delivered updates:

1. Added asset list usability controls:
   - Search input
   - Status/class/site filters
   - Sort options (updated time, name, id)
2. Added result feedback for asset exploration:
   - "showing X of Y" summary
   - loading and filtered-empty states
   - one-click filter reset actions
3. Improved relation workflow clarity and safety:
   - Selected source asset context in section header
   - Source details summary (class/status/ip)
   - Validation to prevent self-relations
   - Delete confirmation before destructive action
   - Success feedback after create/delete operations
4. Added explicit non-blocking read-only guidance inside relation operations.
5. Updated UI copy and localization keys in `en-US` bundle for the new flows.

Validation:

- `npm run build` (`apps/web-console`)
- `cargo check --workspace`
