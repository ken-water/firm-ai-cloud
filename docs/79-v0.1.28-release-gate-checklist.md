# v0.1.28 Release Gate Checklist

Date: 2026-03-20
Version: v0.1.28

## 1. Preconditions

1. API and web-console run against migrated database.
2. `v0.1.27` release is complete and synchronized.
3. Demo/stakeholder accounts are available for overview and large-screen routes.
4. `v0.1.28` scope is mapped to:
   - `docs/62-smb-intelligent-cloud-product-strategy-baseline.md`

## 2. Required Planning Evidence

1. Planning issue maps flow shortcuts, demo control baseline, and state-cue outcomes.
2. Scope defines user-visible improvements for non-technical operators.
3. Scope defines acceptance artifact expectations for operator-flow scenarios.

## 3. Required Validation Coverage

1. Flow shortcuts are validated for reduced context switching in overview workflows.
2. Demo control baseline is validated for run-state visibility and deterministic controls.
3. State cue harmonization is validated for ready/attention/blocking consistency.
4. Operator-flow acceptance contract is validated in release evidence.

## 4. Required Artifacts

- `summary.json`
- `summary.md`
- `artifact-index.json`
- stage logs (`*.log`)

Release notes metadata must include:

- `- Demo acceptance artifacts: \`<summary.json path>, <artifact-index.json path>\``
- `- Role cockpit acceptance artifacts: \`<summary.json path>, <artifact-index.json path>\``
- `- Operator flow acceptance artifacts: \`<summary.json path>, <artifact-index.json path>\``

## 5. Release Gates

Before publish:

```bash
cargo check --workspace
cargo test -p api
cd apps/web-console && npm run check:ui
```

Publish sync:

```bash
bash scripts/release-github-issues-check.sh --version 0.1.28 --notes-file release-notes/v0.1.28.md
make release-publish-dry VERSION=0.1.28
make release-publish VERSION=0.1.28
make release-check VERSION=0.1.28
```
