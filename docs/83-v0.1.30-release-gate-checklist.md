# v0.1.30 Release Gate Checklist

Date: 2026-03-20
Version: v0.1.30

## 1. Preconditions

1. API and web-console run against migrated database.
2. `v0.1.29` release is complete and synchronized.
3. External cohort sample data is available for rollout board validation.
4. `v0.1.30` scope is mapped to:
   - `docs/62-smb-intelligent-cloud-product-strategy-baseline.md`

## 2. Required Planning Evidence

1. Planning issue maps onboarding guardrails, cohort rollout board, and feedback closure outcomes.
2. Scope defines user-visible simplification for external scale-out operators.
3. Scope defines acceptance artifact expectations for scale-out readiness scenarios.

## 3. Required Validation Coverage

1. Onboarding guardrails are validated for repeated external activation scenarios.
2. Cohort rollout board is validated for blocking-first prioritization.
3. Adoption feedback closure queue is validated for owner/status/timestamp continuity.
4. Scale-out acceptance contract is validated in release evidence.

## 4. Required Artifacts

- `summary.json`
- `summary.md`
- `artifact-index.json`
- stage logs (`*.log`)

Release notes metadata must include:

- `- Demo acceptance artifacts: \`<summary.json path>, <artifact-index.json path>\``
- `- Role cockpit acceptance artifacts: \`<summary.json path>, <artifact-index.json path>\``
- `- Operator flow acceptance artifacts: \`<summary.json path>, <artifact-index.json path>\``
- `- External readiness acceptance artifacts: \`<summary.json path>, <artifact-index.json path>\``
- `- Scale-out acceptance artifacts: \`<summary.json path>, <artifact-index.json path>\``

## 5. Release Gates

Before publish:

```bash
cargo check --workspace
cargo test -p api
cd apps/web-console && npm run check:ui
```

Publish sync:

```bash
bash scripts/release-github-issues-check.sh --version 0.1.30 --notes-file release-notes/v0.1.30.md
make release-publish-dry VERSION=0.1.30
make release-publish VERSION=0.1.30
make release-check VERSION=0.1.30
```
